include: package:flutter_lints/flutter.yaml

linter:
  rules:
    avoid_print: true
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_locals: true
    unnecessary_this: true
    always_specify_types: false
    prefer_single_quotes: true
    sort_child_properties_last: true
    avoid_unnecessary_containers: true
    avoid_redundant_argument_values: true
    prefer_interpolation_to_compose_strings: true
    unnecessary_string_interpolations: true
    use_key_in_widget_constructors: true
    prefer_const_constructors_in_immutables: true
    sized_box_for_whitespace: true
    avoid_returning_null_for_void: true
    cancel_subscriptions: true
    close_sinks: true

analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    # Generated exact translation catalogs are compile-checked by
    # test/localization_coverage_test.dart. Excluding them keeps the analysis
    # server responsive while it analyzes the handwritten application code.
    - "lib/core/localization/generated_translations/**"
  errors:
    invalid_assignment: warning
    missing_return: error
    dead_code: info
