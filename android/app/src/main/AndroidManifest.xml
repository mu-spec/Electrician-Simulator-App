<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Needed by google_fonts, connectivity_plus, share_plus, url_launcher -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Needed by Phase 3F resistor scanner reference photo capture -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Declares that the app reads the Android advertising ID.
         The google_mobile_ads SDK merges this automatically; declaring it
         explicitly keeps the Play Console "Does your app use advertising ID?"
         answer (Yes) visible in source. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"/>

    <application
        android:label="Electrician Simulator App"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Google AdMob App ID.
             WARNING: this value must NEVER be empty. The Mobile Ads SDK reads it
             at process start via MobileAdsInitProvider, before Flutter loads. An
             empty or malformed value throws IllegalStateException and the app
             crashes on launch with "Invalid application ID".

             The value below is Google's PUBLIC TEST App ID. It is safe for
             development but earns no revenue. Replace it with the real AdMob App
             ID for com.koreappstek.ElectricianSimulatorApp before release. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value=/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
