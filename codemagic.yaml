workflows:
  android-release:
    name: Android Release Build (arm64 + arm32)
    instance_type: mac_mini_m1
    max_build_duration: 60

    environment:
      groups:
        - keystore_credentials
      flutter: stable
      xcode: latest
      cocoapods: default

    cache:
      cache_paths:
        - $FLUTTER_ROOT/.pub-cache
        - $HOME/.gradle/caches

    scripts:
      - name: Flutter version
        script: |
          flutter --version

      - name: Clean project
        script: |
          flutter clean
          rm -rf build/

      - name: Get Flutter packages
        script: |
          flutter pub get

      - name: Verify assets cleaned (size optimization)
        script: |
          echo "=== assets/images/ ==="
          ls -lh assets/images/ || true
          echo "Should contain app_icon.png and tutorial_*.png"
          # Fail if large PNGs still present
                    if [ -f assets/images/app_icon_new.png ]; then echo "ERROR: app_icon_new.png still exists"; exit 1; fi
          if [ -f assets/images/splash_logo.png ]; then echo "ERROR: splash_logo.png still exists"; exit 1; fi
          echo "=== assets/l10n/ check ==="
          ls -1 assets/l10n/*.json.gz | wc -l || true
          echo "Should show exactly 49 compressed translation asset files"

      - name: Analyze project
        script: |
          flutter analyze --no-fatal-infos --no-fatal-warnings || true

      - name: Verify 50-language localization coverage
        script: |
          python3 scripts/localization_audit.py

      - name: Verify production quiz quality
        script: |
          python3 scripts/quiz_audit.py

      - name: Verify production wiring content
        script: |
          python3 scripts/wiring_audit.py

      - name: Run tests
        script: |
          flutter test

      - name: Decode keystore if available
        script: |
          if [ -n "$CM_KEYSTORE" ]; then
            echo "$CM_KEYSTORE" | base64 --decode > "$CM_BUILD_DIR/android/app/key.jks"
            cat > "$CM_BUILD_DIR/android/key.properties" << EOF
          storePassword=$CM_KEYSTORE_PASSWORD
          keyPassword=$CM_KEY_PASSWORD
          keyAlias=$CM_KEY_ALIAS
          storeFile=key.jks
          EOF
            echo "✅ SUCCESS: Keystore decoded and key.properties generated successfully!"
            echo "The app will be signed with the production release certificate."
          else
            echo "❌ WARNING: No keystore provided in Environment Variables."
            echo "Building unsigned/debug artifacts."
          fi

      # Optimized localization architecture: 49 languages stored in assets/l10n/*.json.gz
      # Eliminates 46MB of static Dart string maps from libapp.so (saves ~15-20MB in APK)
      # Builds one APK per ABI: arm64-v8a (64-bit) and armeabi-v7a (32-bit).
      # x86 / x86_64 are deliberately excluded - those target emulators and a
      # small number of Chromebooks, not real phones.
      - name: Build release APKs (arm64-v8a + armeabi-v7a)
        script: |
          flutter build apk --release --split-per-abi --target-platform android-arm64,android-arm --obfuscate --split-debug-info=./symbols
          echo "=== APK sizes (expect one arm64-v8a and one armeabi-v7a) ==="
          ls -lh build/app/outputs/flutter-apk/*.apk
          du -sh build/app/outputs/flutter-apk/

      - name: Build release App Bundle (arm64-v8a + armeabi-v7a)
        script: |
          flutter build appbundle --release --target-platform android-arm64,android-arm --obfuscate --split-debug-info=./symbols
          echo "=== AAB size ==="
          ls -lh build/app/outputs/bundle/release/*.aab
          echo "The AAB now carries native libraries for both ARM ABIs, so the"
          echo "uploaded file is larger than the previous arm64-only bundle."
          echo "User download size is UNCHANGED: Play generates a per-device APK"
          echo "containing only that device ABI."

    artifacts:
      - build/app/outputs/flutter-apk/*arm64-v8a*.apk
      - build/app/outputs/flutter-apk/*armeabi-v7a*.apk
      - build/app/outputs/flutter-apk/*release*.apk
      - build/app/outputs/bundle/release/*.aab

    publishing:
      email:
        recipients:
          - your-email@example.com
        notify:
          success: true
          failure: true